<views:BaseScreenView x:Class="CRSim.ScreenSimulator.Views.BeijingNan.SecondaryPlatformScreenView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:pp="https://www.cnblogs.com/pumbaa"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CRSim.ScreenSimulator.Views.BeijingNan"
        mc:Ignorable="d"  Foreground="#df3a38"
        xmlns:converters="clr-namespace:CRSim.ScreenSimulator.Converters"
        xmlns:converters1="clr-namespace:CRSim.Shared.Converters;assembly=CRSim.Shared"
        FontFamily="黑体" FontSize="25" FontWeight="Bold"
        Title="引导屏" SizeToContent="WidthAndHeight"
        WindowStyle="None" AllowsTransparency="True" Background="Black"
        xmlns:views="clr-namespace:CRSim.ScreenSimulator.Views"
        ResizeMode="NoResize">
    <Window.Resources>
        <converters1:DateTimeToStringConverter x:Key="DateTimeToStringConverter" Format="HH:mm" />
        <converters1:EmptyToVisibilityConverter x:Key="EmptyToVisibilityConverter" />
        <converters:LocationToStringConverter x:Key="LocationToStringConverter" DisplayMode="least" DisplayArrow="False" HyphenText="-"/>
    </Window.Resources>
    <StackPanel Orientation="Horizontal">
        <Grid Margin="4" Background="Black" MinWidth="350" MinHeight="150">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="#df3a38"/>
                    </Style>
                </Grid.Resources>
                <TextBlock Grid.Column="0" Text="{Binding LeftScreen[0].TrainNumber}" VerticalAlignment="Center" TextAlignment="Left" Margin="80,0,0,0"/>
                <TextBlock Grid.Column="1" Text="次" Margin="4,0,10,0" VerticalAlignment="Center" TextAlignment="Left"/>
                <TextBlock Grid.Column="2" Text="{Binding LeftScreen[0].DepartureTime,Converter={StaticResource DateTimeToStringConverter}}" VerticalAlignment="Center" TextAlignment="Right"/>
                <TextBlock Grid.Column="3" Text="开" Margin="4,0,80,0" VerticalAlignment="Center" TextAlignment="Right"/>
            </Grid>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="#eef4c9"/>
                    </Style>
                </Grid.Resources>
            <TextBlock Grid.Column="1" VerticalAlignment="Center" TextAlignment="Left" Margin="0,0,10,0">
                <TextBlock.Text>
                    <MultiBinding Mode="OneWay" Converter="{StaticResource LocationToStringConverter}">
                        <Binding Path="LeftScreen[0].Length" />
                        <Binding Path="Location" />
                        <Binding Source="left" />
                        <Binding Source="closed" />
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
            <TextBlock Grid.Column="2" Text="←" VerticalAlignment="Center" TextAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock Grid.Column="3" Text="{Binding Location}" VerticalAlignment="Center" TextAlignment="Center"/>
            <TextBlock Grid.Column="4" Text="→" VerticalAlignment="Center" TextAlignment="Center" Margin="10,0,0,0"/>
            <TextBlock Grid.Column="5" VerticalAlignment="Center" TextAlignment="Right" Margin="10,0,0,0">
                <TextBlock.Text>
                    <MultiBinding Mode="OneWay" Converter="{StaticResource LocationToStringConverter}">
                        <Binding Path="LeftScreen[0].Length" />
                        <Binding Path="Location" />
                        <Binding Source="right" />
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
            </Grid>
        </Grid>
    </StackPanel>
</views:BaseScreenView>
