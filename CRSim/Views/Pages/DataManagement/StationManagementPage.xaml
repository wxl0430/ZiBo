<Page
    x:Class="CRSim.Views.StationManagementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:CRSim.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:CRSim.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="clr-namespace:CRSim.Shared.Converters;assembly=CRSim.Shared"
    Title="StationManagementPage"
    d:DesignHeight="450"
    d:DesignWidth="800"
    FontFamily="Microsoft YaHei"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d"
   >
    <Page.Resources>
        <converters:TimeSpanToStringConverter x:Key="TimeSpanToStringConverter" />
        <converters:ListToStringConverter x:Key="ListToStringConverter" Separator=", " />
        <Style BasedOn="{StaticResource {x:Type DataGrid}}" x:Key="CustomDataGridStyle" TargetType="DataGrid">
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                            </Trigger>
                        </Style.Triggers>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="RowStyle">
                <Setter.Value>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource CardStrokeColorDefaultBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="GridLinesVisibility" Value="Vertical"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
        </Style>
    </Page.Resources>
    <Grid x:Name="ContentPagePane" Height="Auto">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <controls:PageHeader Margin="0,0,0,32" Title="{Binding ViewModel.PageTitle}" Description="{Binding ViewModel.PageDescription}" />
        <Grid Grid.Row="1" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Menu Grid.Row="0" Margin="0,0,0,8">
                    <MenuItem>
                        <MenuItem.Header>
                            <TextBlock Focusable="False" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                           FontSize="16" Text="&#xE710;" VerticalAlignment="Center"/>
                        </MenuItem.Header>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction Command="{Binding ViewModel.AddStationCommand}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </MenuItem>
                    <MenuItem>
                        <MenuItem.Header>
                            <TextBlock Focusable="False" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                           FontSize="16" Text="&#xE711;" VerticalAlignment="Center"/>
                        </MenuItem.Header>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction Command="{Binding ViewModel.DeleteStationCommand}" CommandParameter="{Binding SelectedItem,ElementName=StationsList}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </MenuItem>
                    <Separator/>
                    <MenuItem>
                        <MenuItem.Header>
                            <TextBlock Focusable="False" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                           FontSize="16" Text="&#xE74D;" VerticalAlignment="Center"/>
                        </MenuItem.Header>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction Command="{Binding ViewModel.DeleteAllStationsCommand}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </MenuItem>
                    <MenuItem>
                        <MenuItem.Header>
                            <TextBlock Focusable="False" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                               FontSize="16" Text="&#xE8B6;" VerticalAlignment="Center"/>
                        </MenuItem.Header>
                        <MenuItem Header="从模拟广播系统导入" FontSize="14">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding ViewModel.ImportFrom7DCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </MenuItem>
                    </MenuItem>
                </Menu>
                <Border Grid.Row="1" Background="{DynamicResource ExpanderHeaderBackground}" CornerRadius="{DynamicResource ControlCornerRadius}"
                            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <ScrollViewer VerticalAlignment="Stretch">
                        <ListView PreviewMouseWheel="StationsList_PreviewMouseWheel" x:Name="StationsList" ItemsSource="{Binding ViewModel.StationNames}"  SelectionMode="Single">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding ViewModel.StationSelectedCommand}"
                                                           CommandParameter="{Binding SelectedItem,ElementName=StationsList}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding}" Style="{StaticResource BodyTextBlockStyle}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </ScrollViewer>
                </Border>
            </Grid>
            <ScrollViewer Grid.Column="1" Margin="0,0,0,24" Padding="0,0,24,0">
                <Grid Margin="12,0,0,0" IsEnabled="{Binding ViewModel.IsSelected}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Expander Grid.Row="0">
                        <Expander.Header>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Width="20" Height="20" Margin="10,5,10,5" 
                                           VerticalAlignment="Center" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                           Text="&#xE946;" FontSize="20"/>

                                <StackPanel Grid.Column="1" Margin="12" Orientation="Vertical">
                                    <TextBlock Text="信息一览" Style="{StaticResource BodyTextBlockStyle}"/>
                                    <TextBlock Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}">
                                        概览车站设施、车次等信息
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </Expander.Header>
                        <Expander.Content>
                            <Grid Margin="48,0,48,0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="车站名称：" Margin="0,0,12,12"
                                           Style="{StaticResource BodyTextBlockStyle}"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding ViewModel.SelectedStation.Name}"
                                           Style="{StaticResource DetailTextBlockStyle}"/>
                                <TextBlock Grid.Row="1" Grid.Column="0" Text="列车停靠数：" Margin="0,0,12,12"
                                           Style="{StaticResource BodyTextBlockStyle}"/>
                                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding ViewModel.TrainStops.Count}"
                                           Style="{StaticResource DetailTextBlockStyle}"/>
                                <TextBlock Grid.Row="2" Grid.Column="0" Text="候车室数：" Margin="0,0,12,12"
                                           Style="{StaticResource BodyTextBlockStyle}"/>
                                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding ViewModel.WaitingAreaNames.Count}"
                                           Style="{StaticResource DetailTextBlockStyle}"/>
                                <TextBlock Grid.Row="3" Grid.Column="0" Text="检票口数：" Margin="0,0,12,12"
                                           Style="{StaticResource BodyTextBlockStyle}"/>
                                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding ViewModel.TicketChecks.Count}"
                                           Style="{StaticResource DetailTextBlockStyle}"/>
                                <TextBlock Grid.Row="4" Grid.Column="0" Text="站台数：" Margin="0,0,12,0"
                                           Style="{StaticResource BodyTextBlockStyle}"/>
                                <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding ViewModel.Platforms.Count}"
                                           Style="{StaticResource DetailTextBlockStyle}"/>
                            </Grid>
                        </Expander.Content>
                    </Expander>
                    <Expander Margin="0,4,0,0" Grid.Row="1">
                        <Expander.Header>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Width="20" Height="20" Margin="10,5,10,5" 
                                           VerticalAlignment="Center" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                           Text="&#xECA5;" FontSize="20"/>

                                <StackPanel Grid.Column="1" Margin="12" Orientation="Vertical">
                                    <TextBlock Text="候车室" Style="{StaticResource BodyTextBlockStyle}"/>
                                    <TextBlock Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}">
                                        管理车站候车室设施
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </Expander.Header>
                        <Expander.Content>
                            <StackPanel Orientation="Vertical" Margin="48,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Menu Grid.Column="1" HorizontalContentAlignment="Right">
                                        <MenuItem>
                                            <MenuItem.Header>
                                                <TextBlock Focusable="False" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                           FontSize="16" Text="&#xE710;" VerticalAlignment="Center"/>
                                            </MenuItem.Header>
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <i:InvokeCommandAction Command="{Binding ViewModel.AddWaitingAreaCommand}"/>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </MenuItem>
                                        <MenuItem>
                                            <MenuItem.Header>
                                                <TextBlock Focusable="False" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                           FontSize="16" Text="&#xE711;" VerticalAlignment="Center"/>
                                            </MenuItem.Header>
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <i:InvokeCommandAction Command="{Binding ViewModel.DeleteWaitingAreaCommand}" CommandParameter="{Binding SelectedItem,ElementName=WaitingAreasList}"/>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </MenuItem>
                                        <Separator/>
                                        <MenuItem>
                                            <MenuItem.Header>
                                                <TextBlock Focusable="False" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                           FontSize="16" Text="&#xE74D;" VerticalAlignment="Center"/>
                                            </MenuItem.Header>
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <i:InvokeCommandAction Command="{Binding ViewModel.DeleteAllWaitingAreaCommand}"/>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </MenuItem>
                                    </Menu>

                                </Grid>
                                <ScrollViewer MaxHeight="250">
                                    <DataGrid Style="{StaticResource CustomDataGridStyle}" ItemsSource="{Binding ViewModel.WaitingAreaNames}"
                                          x:Name="WaitingAreasList" PreviewMouseWheel="WaitingAreasList_PreviewMouseWheel">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Binding="{Binding .}">
                                                <DataGridTextColumn.Header>
                                                    <TextBlock Text="候车室" Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}"/>
                                                </DataGridTextColumn.Header>
                                            </DataGridTextColumn>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </ScrollViewer>
                            </StackPanel>

                        </Expander.Content>
                    </Expander>
                    <Expander Margin="0,4,0,0" Grid.Row="2">
                        <Expander.Header>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Width="20" Height="20" Margin="10,5,10,5" 
                                           VerticalAlignment="Center" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                           Text="&#xF161;" FontSize="20"/>

                                <StackPanel Grid.Column="1" Margin="12" Orientation="Vertical">
                                    <TextBlock Text="检票口" Style="{StaticResource BodyTextBlockStyle}"/>
                                    <TextBlock Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}">
                                        管理车站检票口设施
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </Expander.Header>
                        <Expander.Content>
                            <StackPanel Orientation="Vertical" Margin="48,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Menu Grid.Column="1" HorizontalContentAlignment="Right">
                                        <MenuItem>
                                            <MenuItem.Header>
                                                <TextBlock Focusable="False" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                           FontSize="16" Text="&#xE710;" VerticalAlignment="Center"/>
                                            </MenuItem.Header>
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <i:InvokeCommandAction Command="{Binding ViewModel.AddTicketCheckCommand}"/>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </MenuItem>
                                        <MenuItem>
                                            <MenuItem.Header>
                                                <TextBlock Focusable="False" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                           FontSize="16" Text="&#xE711;" VerticalAlignment="Center"/>
                                            </MenuItem.Header>
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <i:InvokeCommandAction Command="{Binding ViewModel.DeleteTicketCheckCommand}" CommandParameter="{Binding SelectedItem,ElementName=TicketChecksList}"/>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </MenuItem>
                                        <Separator/>
                                        <MenuItem>
                                            <MenuItem.Header>
                                                <TextBlock Focusable="False" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                           FontSize="16" Text="&#xE74D;" VerticalAlignment="Center"/>
                                            </MenuItem.Header>
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <i:InvokeCommandAction Command="{Binding ViewModel.DeleteAllTicketCheckCommand}"/>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </MenuItem>

                                    </Menu>

                                </Grid>
                                <ScrollViewer MaxHeight="250">
                                    <DataGrid Style="{StaticResource CustomDataGridStyle}" ItemsSource="{Binding ViewModel.TicketChecks}"
                                          x:Name="TicketChecksList" PreviewMouseWheel="TicketChecksList_PreviewMouseWheel">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Binding="{Binding WaitingAreaName}">
                                                <DataGridTextColumn.Header>
                                                    <TextBlock Text="候车区" Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}"/>
                                                </DataGridTextColumn.Header>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Binding="{Binding Name}">
                                                <DataGridTextColumn.Header>
                                                    <TextBlock Text="检票口" Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}"/>
                                                </DataGridTextColumn.Header>
                                            </DataGridTextColumn>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </ScrollViewer>
                            </StackPanel>

                        </Expander.Content>
                    </Expander>
                    <Expander Margin="0,4,0,0" Grid.Row="3">
                        <Expander.Header>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Width="20" Height="20" Margin="10,5,10,5" 
                                           VerticalAlignment="Center" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                           Text="&#xE799;" FontSize="20"/>

                                <StackPanel Grid.Column="1" Margin="12" Orientation="Vertical">
                                    <TextBlock Text="站台" Style="{StaticResource BodyTextBlockStyle}"/>
                                    <TextBlock Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}">
                                        管理车站站台设施
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </Expander.Header>
                        <Expander.Content>
                            <StackPanel Orientation="Vertical" Margin="48,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Menu Grid.Column="1" HorizontalContentAlignment="Right">
                                        <MenuItem>
                                            <MenuItem.Header>
                                                <TextBlock Focusable="False" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                           FontSize="16" Text="&#xE710;" VerticalAlignment="Center"/>
                                            </MenuItem.Header>
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <i:InvokeCommandAction Command="{Binding ViewModel.AddPlatformCommand}"/>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </MenuItem>
                                        <MenuItem>
                                            <MenuItem.Header>
                                                <TextBlock Focusable="False" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                           FontSize="16" Text="&#xE711;" VerticalAlignment="Center"/>
                                            </MenuItem.Header>
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <i:InvokeCommandAction Command="{Binding ViewModel.DeletePlatformCommand}" CommandParameter="{Binding SelectedItem,ElementName=PlatformsList}"/>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </MenuItem>
                                        <Separator/>
                                        <MenuItem>
                                            <MenuItem.Header>
                                                <TextBlock Focusable="False" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                           FontSize="16" Text="&#xE74D;" VerticalAlignment="Center"/>
                                            </MenuItem.Header>
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <i:InvokeCommandAction Command="{Binding ViewModel.DeleteAllPlatformCommand}"/>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </MenuItem>
                                    </Menu>

                                </Grid>
                                <ScrollViewer MaxHeight="250">
                                    <DataGrid Style="{StaticResource CustomDataGridStyle}" ItemsSource="{Binding ViewModel.Platforms}"
                                          x:Name="PlatformsList" PreviewMouseWheel="PlatformsList_PreviewMouseWheel">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Binding="{Binding Name}">
                                                <DataGridTextColumn.Header>
                                                    <TextBlock Text="站台" Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}"/>
                                                </DataGridTextColumn.Header>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Binding="{Binding Length}">
                                                <DataGridTextColumn.Header>
                                                    <TextBlock Text="长度" Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}"/>
                                                </DataGridTextColumn.Header>
                                            </DataGridTextColumn>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </ScrollViewer>
                            </StackPanel>

                        </Expander.Content>
                    </Expander>
                    <Expander Margin="0,4,0,0" Grid.Row="4">
                        <Expander.Header>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Width="20" Height="20" Margin="10,5,10,5" 
                                           VerticalAlignment="Center" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                           Text="&#xE932;" FontSize="20"/>
                                <StackPanel Grid.Column="1" Margin="12" Orientation="Vertical">
                                    <TextBlock Text="车站车次" Style="{StaticResource BodyTextBlockStyle}"/>
                                    <TextBlock Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}">
                                        编辑此车站的车次
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </Expander.Header>
                        <Expander.Content>
                            <StackPanel Orientation="Vertical" Margin="48,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Menu Grid.Column="1" HorizontalContentAlignment="Right">
                                        <MenuItem>
                                            <MenuItem.Header>
                                                <TextBlock Focusable="False" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                           FontSize="16" Text="&#xE710;" VerticalAlignment="Center"/>
                                            </MenuItem.Header>
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <i:InvokeCommandAction Command="{Binding ViewModel.AddTrainStopCommand}"/>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </MenuItem>
                                        <MenuItem>
                                            <MenuItem.Header>
                                                <TextBlock Focusable="False" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                           FontSize="16" Text="&#xE70F;" VerticalAlignment="Center"/>
                                            </MenuItem.Header>
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <i:InvokeCommandAction Command="{Binding ViewModel.EditTrainStopCommand}" CommandParameter="{Binding SelectedItem,ElementName=TrainStopsList}"/>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </MenuItem>
                                        <MenuItem>
                                            <MenuItem.Header>
                                                <TextBlock Focusable="False" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                           FontSize="16" Text="&#xE711;" VerticalAlignment="Center"/>
                                            </MenuItem.Header>
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <i:InvokeCommandAction Command="{Binding ViewModel.DeleteTrainStopCommand}" CommandParameter="{Binding SelectedItem,ElementName=TrainStopsList}"/>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </MenuItem>
                                        <Separator/>
                                        <MenuItem>
                                            <MenuItem.Header>
                                                <TextBlock Focusable="False" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                           FontSize="16" Text="&#xE74D;" VerticalAlignment="Center"/>
                                            </MenuItem.Header>
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <i:InvokeCommandAction Command="{Binding ViewModel.DeleteAllTrainStopCommand}"/>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </MenuItem>
                                        <MenuItem>
                                            <MenuItem.Header>
                                                <TextBlock Focusable="False" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                           FontSize="16" Text="&#xEDE1;" VerticalAlignment="Center"/>
                                            </MenuItem.Header>
                                            <MenuItem Header="导出到Excel表格" FontSize="14">
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="Click">
                                                        <i:InvokeCommandAction Command="{Binding ViewModel.ExportToExcelCommand}"/>
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                            </MenuItem>
                                        </MenuItem>
                                        <MenuItem>
                                            <MenuItem.Header>
                                                <TextBlock Focusable="False" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                           FontSize="16" Text="&#xE8B6;" VerticalAlignment="Center"/>
                                            </MenuItem.Header>
                                            <MenuItem Header="联网获取" FontSize="14">
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="Click">
                                                        <i:InvokeCommandAction Command="{Binding ViewModel.ImportFromInternetCommand}"/>
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                            </MenuItem>
                                            <MenuItem Header="从路路通导入" FontSize="14">
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="Click">
                                                        <i:InvokeCommandAction Command="{Binding ViewModel.ImportFromLulutongCommand}"/>
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                            </MenuItem>
                                            <MenuItem Header="从Excel表格导入" FontSize="14">
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="Click">
                                                        <i:InvokeCommandAction Command="{Binding ViewModel.ImportFromExcelCommand}"/>
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                            </MenuItem>
                                            <MenuItem Header="从车次数据库导入" FontSize="14">
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="Click">
                                                        <i:InvokeCommandAction Command="{Binding ViewModel.ImportFromTrainNumbersCommand}"/>
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                            </MenuItem>
                                        </MenuItem>
                                    </Menu>
                                </Grid>
                                <ScrollViewer MaxHeight="250">
                                    <DataGrid Style="{StaticResource CustomDataGridStyle}" ItemsSource="{Binding ViewModel.TrainStops}"
                                          x:Name="TrainStopsList" PreviewMouseWheel="TrainStopsList_PreviewMouseWheel">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Binding="{Binding Number}">
                                                <DataGridTextColumn.Header>
                                                    <TextBlock Text="车次" Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}"/>
                                                </DataGridTextColumn.Header>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Binding="{Binding Length}">
                                                <DataGridTextColumn.Header>
                                                    <TextBlock Text="长度" Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}"/>
                                                </DataGridTextColumn.Header>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Binding="{Binding ArrivalTime,Converter={StaticResource TimeSpanToStringConverter}}">
                                                <DataGridTextColumn.Header>
                                                    <TextBlock Text="到时" Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}"/>
                                                </DataGridTextColumn.Header>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Binding="{Binding DepartureTime,Converter={StaticResource TimeSpanToStringConverter}}">
                                                <DataGridTextColumn.Header>
                                                    <TextBlock Text="发时" Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}"/>
                                                </DataGridTextColumn.Header>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Binding="{Binding TicketChecks,Converter={StaticResource ListToStringConverter}}">
                                                <DataGridTextColumn.Header>
                                                    <TextBlock Text="检票口" Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}"/>
                                                </DataGridTextColumn.Header>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Binding="{Binding Platform}">
                                                <DataGridTextColumn.Header>
                                                    <TextBlock Text="站台" Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}"/>
                                                </DataGridTextColumn.Header>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Binding="{Binding Landmark}">
                                                <DataGridTextColumn.Header>
                                                    <TextBlock Text="地标" Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}"/>
                                                </DataGridTextColumn.Header>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Binding="{Binding Origin}">
                                                <DataGridTextColumn.Header>
                                                    <TextBlock Text="始发站" Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}"/>
                                                </DataGridTextColumn.Header>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Binding="{Binding Terminal}">
                                                <DataGridTextColumn.Header>
                                                    <TextBlock Text="终到站" Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}"/>
                                                </DataGridTextColumn.Header>
                                            </DataGridTextColumn>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </ScrollViewer>
                            </StackPanel>
                        </Expander.Content>
                    </Expander>
                    <Border Grid.Row="5" Background="{DynamicResource ExpanderHeaderBackground}" Margin="0,4,0,0"
                            BorderBrush="{DynamicResource ExpanderHeaderBorderBrush}" BorderThickness="{StaticResource ExpanderBorderThemeThickness}"
                            Padding="{StaticResource ExpanderPadding}" CornerRadius="{DynamicResource ControlCornerRadius}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Width="20" Height="20" Text="&#xE74E;" FontSize="20"
                                       Margin="10,5,10,5" VerticalAlignment="Center" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                            <StackPanel
                                Grid.Column="1" Margin="12" Orientation="Vertical">
                                <TextBlock Text="保存车站配置" FontSize="14" Style="{StaticResource BodyTextBlockStyle}"/>
                                <TextBlock Opacity="0.7" FontSize="12" Style="{StaticResource CaptionTextBlockStyle}">验证并保存数据配置</TextBlock>
                            </StackPanel>
                            <Button HorizontalAlignment="Right" Margin="10" Grid.Column="2" Command="{Binding ViewModel.SaveChangesCommand}"
                                    FontSize="14" Style="{DynamicResource AccentButtonStyle}" Content="保存配置" />
                        </Grid>

                    </Border>
                </Grid>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
