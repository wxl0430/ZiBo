<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CRSim.Views.StartWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.Background>
            <ImageBrush x:Name="BackImage" ImageSource="/Assets/Background/SplashScreenBackground.png" Stretch="UniformToFill"/>
        </Grid.Background>
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,10,10,10">
            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center" Orientation="Horizontal" Margin="0,40,0,0">
                <ProgressRing IsActive="True" Width="20" Height="20" Foreground="DeepSkyBlue" IsIndeterminate="True"/>
                <TextBlock Text="Loading..." FontSize="24" HorizontalAlignment="Center" Margin="10,0,0,0" />
            </StackPanel>
            <ProgressBar x:Name="progressBar" Width="400" Height="8" Margin="0,40,0,0" Foreground="#0078D7" IsIndeterminate="True"/>
        </StackPanel>
    </Grid>

</Window>
