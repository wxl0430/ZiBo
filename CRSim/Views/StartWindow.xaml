<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CRSim.Views.StartWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:local="using:CRSim.Views"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:CRSim.Converters"     
    xmlns:converters1="using:CommunityToolkit.WinUI.Converters"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"   
    xmlns:winui="using:CommunityToolkit.WinUI" 
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <MediaPlayerElement x:Name="Logo" Source="ms-appx:///Assets/Background/CRSim.wmv" AutoPlay="True" Margin="0,0,0,0"/>
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,45,10,10">
            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center" Orientation="Horizontal" Margin="8,48,0,0">
                <ProgressRing IsActive="True" Width="35" Height="35" Foreground="DeepSkyBlue" IsIndeterminate="True" />
            </StackPanel>
            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center" Orientation="Horizontal">
                <ProgressBar x:Name="progressBar" Width="500" Height="10" Margin="0,55,0,0" Foreground="#0078D7" IsIndeterminate="True"/>
                <TextBlock Text="{x:Bind AppVersion}" Margin="10,50,0,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>

</Window>
